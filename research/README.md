# Research archive note

`research/experiments/` 与 `research/history/` 保留旧实验计划、失败记录、复现说明、结果和当时的设计文档。为了不改写既有实验结论，正文中的旧包名、旧脚本和旧输出路径保持原样。

这些文档中出现的 `output/runs/`、`output/trf_runs/`、`output/trf_target_runs/`、`output/trf_full_runs/`、`output/instance_discriminator_runs/` 以及旧 `archive/` 路径均为历史引用。相应运行产物已清理，旧命令不再是项目接口；当前命令以根目录 `README.md` 和三个模块 README 为准。
