# 2 Helpfulness-based prediction（示例判别器）

## 2.1 scripts/instance_discriminator/run.ps1（示例判别器统一运行入口）

### Input

- 运行配置：`config/instance_discriminator.json`
- 目标句子文件：通过 `-Targets` 指定，支持 JSON 或 JSONL
- 候选示例文件：通过 `-Candidates` 指定
  - 每个目标必须对应 16 条候选示例
  - 候选可以是带 Skill 标注的正例，也可以是不含 Skill 的负例
- 可选目标特征文件：通过 `-Features` 指定
  - 可以包含目标的实体类型判断和 TRF
  - 不提供时，示例判别器只使用目标句子和候选示例
- 运行编号：通过 `-RunId` 指定

### Process

- 将 PowerShell 参数转换为 `runner.py` 的命令行参数
- 支持以下运行方式：
  - `-PrepareOnly`：只检查输入并生成 Prompt，不调用模型
  - `-AllowNetwork`：允许调用 Qwen 接口
  - `-ConfirmFullRun`：确认执行不带 `Limit` 的完整在线任务
  - `-Resume`：恢复未完成的运行
  - `-RetryFailed`：重新执行失败记录，必须和 `-Resume` 一起使用
  - `-Limit`：只处理前若干条目标记录
- 调用 `code/instance_discriminator/runner.py` 执行完整流程
- 如果 Python 程序异常退出，则 PowerShell 脚本终止并报告错误

### Output

- 运行目录：`output/instance_discriminator/<run-id>/`
- 控制台输出本次运行的最终状态：
  - `completed`
  - `partial`
  - `failed`

---

### 2.1.1 runner.py（主流程调度器）

#### Input

- `config/instance_discriminator.json`
- 目标句子文件
- 候选示例文件
- 可选目标特征文件
- 运行控制参数：`run-id`、`limit`、网络权限、恢复模式等

#### Process

- 读取并验证配置
- 加载目标、候选和可选特征，并按照 `(dataset_id, record_id)` 对齐
- 记录所有输入文件的路径、SHA256 和目标集合，形成不可变输入快照
- 依次执行四个阶段：

  1. `prepare`
     - 规范化目标和 16 条候选示例
     - 构建示例有用性判别 Prompt

  2. `discriminate`
     - 调用 Qwen 对每条候选示例进行有用性判别
     - 严格解析模型输出
     - 对格式错误的响应最多执行两次修复
     - 使用固定规则筛选有帮助的候选示例

  3. `predict_target_skills`
     - 使用筛选后的示例构建最终 Skill 预测 Prompt
     - 调用模型预测目标句子中的精确 Skill 字符跨度
     - 对预测格式或文本对齐错误最多执行两次修复

  4. `diagnostics`
     - 统计判别、筛选、预测及失败情况
     - 生成需要人工检查的样本
     - 根据 3% 校验失败容忍规则确定最终运行状态

- 每个阶段结束后更新 `manifest.json`
- 运行结束前重新检查输入文件是否发生变化
- 记录代码、配置、输入和输出文件的哈希值

#### Output

- `source_snapshot.json`：输入文件快照
- `candidates/records.jsonl`：规范化后的目标和候选
- `prompts/records.jsonl`：有用性判别 Prompt
- `raw/responses.jsonl`：模型原始判别响应
- `parsed/judgments.jsonl`：解析后的候选判别结果
- `selected/records.jsonl`：Eligible 筛选后的候选示例
- `prediction/`：最终 Skill 预测相关产物
- `audit/`：诊断、校验问题和人工复核样本
- `manifest.json`：运行状态和完整哈希账本

---

### 2.1.2 common.py（配置、输入合同和运行目录管理）

#### Input

- 示例判别器配置
- 目标、候选和可选特征文件
- `run-id`
- 已有运行目录及其 Manifest（恢复运行时）

#### Process

- 验证固定配置合同：
  - Pipeline 版本必须为 `instance-discriminator-v5`
  - 每个目标必须有 16 条候选
  - 模型固定为 `qwen3.7-plus-2026-05-26`
  - Temperature 固定为 0
  - 最终预测最多修复两次
  - 校验失败容忍率固定为 3%
- 为缺少中立身份字段的目标补充：
  - `dataset_id`
  - `record_id`
  - `source_sha256`
- 使用 `(dataset_id, record_id)` 对齐目标、候选和特征
- 检查目标与候选、目标与特征中的句子是否一致
- 建立运行目录结构
- 记录配置、输入、依赖版本和实现文件哈希
- 恢复运行时检查：
  - 输入是否一致
  - 配置是否一致
  - 目标集合是否一致
  - 实现代码是否发生变化
- 除受控的 repair 实现升级外，代码发生变化后必须使用新的 `run-id`

#### Output

- 规范化的目标记录
- 按目标身份索引的候选与特征
- `source_snapshot.json`
- 初始化或恢复后的 `manifest.json`
- 各阶段使用的输出目录对象

---

### 2.1.3 pipeline.py（候选规范化、Prompt 构建和 Eligible 筛选）

#### Input

- 一条目标句子
- 与目标对应的 16 条候选示例
- 可选目标特征
- 模型返回的候选判别结果
- `config/instance_discriminator.json` 中的门控参数

#### Process

##### 1. 候选示例规范化

- 检查目标、候选和特征的身份及句子是否一致
- 检查每个目标是否恰好有 16 条候选
- 防止目标自身进入候选集合，即 Leave-One-Out 泄漏
- 检查候选 `demo_idx` 和中立身份是否重复
- 检查候选标签合同：
  - `accepted` 候选必须包含至少一个 Skill 跨度
  - `negative` 候选不能包含 Skill 跨度
  - `negative` 候选不能包含伪 TRF
- 保存候选的：
  - 原始句子
  - Skill 跨度
  - 伪 TRF
  - 向量相似度
  - 实体存在性得分
  - 检索排名

##### 2. 构建有用性判别 Prompt

- 向模型展示目标句子及 16 条候选示例
- 如果提供了特征文件，则额外展示：
  - `entity_types`
  - 目标 TRF
- 要求模型为每个候选返回：
  - `demo_idx`
  - `helpfulness_score`：1–5 分
  - `role`
  - `reason_codes`
- 不允许输出自由文本 `reason`
- 角色受到候选标签约束：
  - 正例只能是 `supporting` 或 `irrelevant`
  - 负例只能是 `contrastive` 或 `irrelevant`

##### 3. 严格解析判别结果

- 响应必须是合法 JSON
- 顶层只能包含 `judgments`
- 必须为 16 个候选各返回一条判断
- `demo_idx` 不能遗漏、重复或新增
- 每条判断只能包含规定的四个字段
- `helpfulness_score` 必须是 1–5 的整数
- `reason_codes` 必须来自受控代码集合

##### 4. Eligible 筛选

- 只有满足以下条件的候选才有资格进入最终示例集：
  - `helpfulness_score > 3`，即只能是 4 分或 5 分
  - `role != irrelevant`
- 保留全部满足 Eligible 条件的候选
- 严格维持候选输入文件中的原始顺序
- 不按照 Helpfulness、实体存在性得分、向量相似度或 `demo_idx` 重新排序
- 不执行总数量、supporting 数量或 contrastive 数量上限截断
- `gate_rank` 只记录 Eligible 候选的原始保留顺序，不表示重新计算后的质量排名
- 少于 2 条时标记为需要复核
- 判别格式最终无法修复时，保留该目标，但使用空示例集合继续后续预测

##### 5. 构建最终 Skill 预测 Prompt

- 只将 Eligible 筛选保留的示例交给最终预测模型
- 每条示例只保留：
  - 句子
  - Skill 跨度
  - Helpfulness 得分
  - supporting/contrastive 角色
- 要求模型返回带可选 `<skill>` 标签的目标原句

#### Output

- `candidate-instances-v1`：规范化候选记录
- `instance-discriminator-prompt-v2`：判别 Prompt
- `instance-judgments-v2`：解析后的逐候选判别
- `instance-selection-v1`：Eligible 筛选后的示例集合
- 最终精确 Skill 预测 Prompt

---

### 2.1.4 online.py（在线判别和结构化响应修复）

#### Input

- 规范化候选记录
- 有用性判别 Prompt
- 已存在的 `raw/responses.jsonl`
- Qwen 客户端和模型参数
- 网络、恢复及失败重试设置

#### Process

- 检查已有原始响应，区分：
  - 已完成记录
  - 已失败记录
  - 待处理记录
- 仅对待处理记录调用 Qwen
- 如果没有显式提供网络权限，则拒绝发送请求
- 对模型响应调用 `parse_judgments`
- 格式不合规时生成受约束的 repair Prompt，其中明确列出：
  - 16 个必需的 `demo_idx`
  - 每个候选允许使用的角色
  - 上一次具体校验错误
- 每条目标最多执行：
  - 1 次初始请求
  - 2 次修复请求
- 每次请求都记录：
  - 请求消息哈希
  - 原始模型响应
  - 解析错误
  - 请求轮次
- 原始响应采用追加写入，恢复运行时以每个目标最后一条记录为准
- 对最终仍无法通过结构校验的响应：
  - 生成 `needs_review` 判别记录
  - 将问题写入 validation issue
  - 使用空示例集合继续预测
- 网络错误、接口错误和运行时错误作为阻断性失败处理

#### Output

- `raw/responses.jsonl`
  - 初始模型响应
  - repair 审计链
  - 最终成功或失败状态
- `parsed/judgments.jsonl`
- 判别阶段校验问题记录
- 提供商或运行时阻断错误记录

---

### 2.1.5 code/common/skill_prediction.py（共享精确 Skill 跨度预测组件）

#### Input

- 由筛选结果构建的最终 Skill 预测 Prompt
- 目标原句
- 模型返回的 `annotated_sentence`
- 最大修复次数和 3% 完成容忍配置

#### Process

- 要求模型返回严格 JSON：

  ```json
  {
    "annotated_sentence": "带有可选 <skill>...</skill> 标签的目标原句"
  }
  ```

- 去除 `<skill>` 标签后，文本必须能够还原为目标原句
- 将 `<skill>` 内文本转换为原句中的精确字符偏移
- 对格式或文本对齐问题最多执行两次修复
- 对少量安全的一对一异常标点进行原句坐标恢复
- 无法安全恢复但仍可解析的结果，保留为模型坐标下的 provisional extraction
- 为每个目标生成一条结果信封，包括失败或缺失的目标
- 按目标合并判别与最终预测的校验问题
- 允许出现校验问题的目标数为：

  `floor(目标数量 × 3%)`

- 网络、缺失响应、运行时和完整性错误不属于可容忍的校验问题

#### Output

- `prediction/prompts.jsonl`：最终预测 Prompt
- `prediction/raw.jsonl`：模型原始响应和修复过程
- `prediction/records.jsonl`：可以正式发布的精确跨度预测
- `prediction/failures.jsonl`：无法正式发布的预测及 provisional extraction
- `prediction/results.jsonl`：覆盖所有目标的结果信封
- `audit/validation_issues.jsonl`：按目标去重后的校验问题账本

---

### 2.1.6 diagnostics.py（运行统计与人工复核抽样）

#### Input

- 所有目标及候选记录
- 判别结果
- 筛选结果
- 原始模型响应
- 最终 Skill 预测
- 校验问题和完成状态

#### Process

- 统计 1–5 分的 Helpfulness 分布
- 统计 supporting、contrastive、irrelevant 的角色分布
- 统计正例和负例的保留比例
- 统计每个目标选择的示例数量
- 计算 Helpfulness 与以下指标的相关性：
  - 向量相似度
  - 实体存在性得分
- 比较目标 TRF 为空和非空时的平均 Helpfulness
- 统计最终预测中的：
  - 正例数量
  - Skill 跨度总数
  - 修复次数
  - 恢复次数
  - 失败和 needs-review 数量
- 优先抽取异常记录，再以固定随机种子抽取普通记录供人工检查
- 不计算 Accuracy、Precision、Recall 或 F1，也不声称筛选提高准确率

#### Output

- `audit/summary.json`
  - 候选判别统计
  - 示例保留率
  - 相关性统计
  - 最终预测统计
  - 失败容忍状态
- `audit/manual_review.jsonl`
  - 人工复核样本
  - 选中示例 ID
  - 预测结果
  - 失败原因和复核原因

---

### 2.1.7 validator.py（运行产物只读验证器）

#### Input

- `config/instance_discriminator.json`
- 待验证的 `run-id`
- 对应运行目录中的 Manifest 和全部产物
- 原始目标、候选和特征文件

#### Process

- 重新检查原始输入文件是否发生变化
- 重新计算实现文件和所有输出文件的 SHA256
- 根据原始输入重新构建：
  - 规范化候选
  - 判别 Prompt
  - 解析结果
  - Eligible 筛选结果
  - 最终预测 Prompt
  - 预测结果信封
  - 校验问题账本
  - 诊断摘要和人工复核样本
- 将重新计算的内容与磁盘产物逐项比较
- 验证 `PrepareOnly` 运行没有产生在线阶段产物
- 验证每个目标都有终态判别响应和预测结果
- 验证最终 `completed` 或 `partial` 状态符合 3% 容忍规则
- 整个验证过程只读，不修改任何运行文件

#### Output

- 控制台 JSON 验证结果，主要包含：
  - `run_id`
  - 验证状态
  - 目标数量
  - 选中示例总数
  - needs-review 数量
  - 预测数量
  - 允许失败数量
  - 实际失败数量
  - 预测出的 Skill 跨度总数
- 验证失败时输出具体的不一致项目

## 2.2 示例判别器的总体输入输出关系

### Input

- 待预测目标句子
- 每个目标对应的 16 条候选示例
- 可选目标实体类型与 TRF 特征

### Process

- 对 16 条候选分别进行 Helpfulness 和角色判别
- 按固定 Eligible 条件保留全部合格示例，并维持原始候选顺序
- 使用筛选后的示例预测目标中的精确 Skill 字符跨度
- 生成诊断、失败审计和完整性验证记录

### Output

- 可供后续第二层或聚合器使用的示例上下文
- 示例专家的独立 Skill 跨度预测
- 覆盖所有目标的结果信封
- 可复现的 Manifest、文件哈希和审计产物
