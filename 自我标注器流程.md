# 1 Self-annotator（自我标注器）

整体流程：

`输入句子 → 生成 Prompt → 每句采样 5 次 → 严格解析跨度 → 存在性投票与跨度聚合 → 输出正式标注 → 完整性验证`

默认配置：

- 配置文件：`config/self_annotator.json`
- 默认输入：`data/raw/skill_sentences.json`
- 默认数据量：326 条
- 正式入口：`scripts/self_annotator/run.ps1`
- 输出目录：`output/self_annotator/<run-id>/`
- 模型：`qwen3.7-plus-2026-05-26`
- 每条句子独立采样 5 次
- Temperature：0.7

## 1.1 scripts/self_annotator/run.ps1（自我标注主流程）

### Input

- `RunId`：本次运行的唯一标识，必填。
- `Input`：待标注的 JSON 文件；未指定时使用配置中的 `data/raw/skill_sentences.json`。
- `Limit`：只处理前 N 条数据，用于小规模测试。
- `Resume`：恢复中断的运行。
- `FailFast`：出现首条不完整响应时立即终止。
- `ConfigPath`：自定义配置文件。
- `PythonExecutable`：指定 Python 解释器。
- `ReaggregateFrom`：不调用模型，使用历史原始响应重新解析和聚合。

### Process

- 计算项目根目录和配置文件路径。
- 普通模式下调用 `code/self_annotator/runner.py`。
- 指定 `ReaggregateFrom` 时改为调用 `code/self_annotator/reaggregate.py`。
- Python 进程返回非零退出码时，PowerShell 脚本抛出异常。

### Output

- 普通模式：`output/self_annotator/<run-id>/`
- 离线重聚合模式：创建新的、不可覆盖的派生运行目录。

### 1.1.1 runner.py（主流程调度器）

#### Input

- 自我标注器配置文件。
- 原始句子 JSON 文件。
- Prompt 模板：`code/self_annotator/prompt_template/skill_annotation.txt`
- 运行参数：`run-id`、`limit`、`resume`、`fail-fast`。

输入数据必须是 JSON 列表，每条记录至少包含：

```json
{
  "idx": 1,
  "sentence": "Work in a way that is patient-centred and inclusive ."
}
```

其中：

- `idx` 必须是唯一整数。
- `sentence` 必须是非空字符串。
- 原始句子不能包含保留标签 `<skill>` 或 `</skill>`。

#### Process

- 加载并校验输入数据。
- 根据 `Limit` 截取记录。
- 加载 Prompt 模板并生成本次运行的 Prompt 快照。
- 按顺序调用：
  1. `ask_qwen.py`
  2. `parse_answers.py`
  3. `consensus.py`
  4. `selection.py`
- 最后调用 `validator.py` 验证整次运行。

#### Output

完整运行目录：

```text
output/self_annotator/<run-id>/
├── prompts/
│   └── records.json
├── raw/
│   ├── responses.jsonl
│   └── summary.json
├── parsed/
│   ├── samples.jsonl
│   └── summary.json
├── aggregated/
│   ├── consensus.jsonl
│   ├── aggregation_audit.jsonl
│   ├── uncertainty_audit.jsonl
│   └── summary.json
├── selected/
│   ├── annotations.json
│   ├── decisions.jsonl
│   └── summary.json
├── manifest.json
└── run.log
```

### 1.1.2 generate_prompts.py（生成标注 Prompt）

#### Input

- 原始句子 JSON 文件。
- Prompt 模板：`code/self_annotator/prompt_template/skill_annotation.txt`
- Prompt 版本：`skill-inline-annotation-v4.1`

#### Process

- 将句子转换为合法的 JSON 字符串，避免引号和特殊字符破坏 Prompt。
- 将模板中的唯一占位符 `<sentence_json>` 替换为当前句子。
- 要求模型：
  - 只标记句子中明确出现的技能或能力。
  - 使用 `<skill>...</skill>` 标记技能跨度。
  - 不修改原句中的任何字符、空格或标点。
  - 没有技能时原样返回句子。
  - 只返回一个包含 `annotated_sentence` 字段的 JSON 对象。

#### Output

Prompt 快照：

```text
output/self_annotator/<run-id>/prompts/records.json
```

每条记录包含：

```json
{
  "idx": 1,
  "sentence": "...",
  "prompt": "...",
  "prompt_version": "skill-inline-annotation-v4.1"
}
```

### 1.1.3 ask_qwen.py（五次独立自我标注）

#### Input

- Prompt 文件：`output/self_annotator/<run-id>/prompts/records.json`
- 原始输入文件，用于校验 Prompt 与原句是否完全对齐。
- 模型及采样配置。
- 环境变量：
  - `DASHSCOPE_API_KEY`
  - `DASHSCOPE_BASE_URL`

#### Process

- 校验配置、Prompt 版本和原始数据对齐关系。
- 根据原始输入文件的 SHA256 生成：
  - `dataset_id`
  - `source_sha256`
  - 输入索引哈希
- 对每条句子串行进行 5 次独立请求。
- 模型调用参数：
  - `temperature=0.7`
  - `max_tokens=1024`
  - `enable_thinking=false`
  - JSON structured output
  - 超时 120 秒
  - 最多重试 5 次
- 单次请求失败时记录错误，不将失败请求伪装成“无技能”结果。
- 一条句子的五次请求状态可能是：
  - `complete`
  - `partial_failed`
  - `failed`
- `Resume` 模式下跳过已经完整完成的句子。
- Manifest 锁定输入、配置、模型、Prompt 和聚合参数，防止恢复时参数漂移。

#### Output

原始模型响应：

```text
output/self_annotator/<run-id>/raw/responses.jsonl
```

每行记录一条句子的：

- 原始句子和 Prompt。
- 五个模型响应。
- 每个响应的状态、正文、token 数、延迟、请求次数和错误信息。

汇总文件：

```text
output/self_annotator/<run-id>/raw/summary.json
```

运行合同：

```text
output/self_annotator/<run-id>/manifest.json
```

运行日志：

```text
output/self_annotator/<run-id>/run.log
```

### 1.1.4 parse_answers.py（严格解析模型回答）

#### Input

```text
output/self_annotator/<run-id>/raw/responses.jsonl
```

#### Process

- 对恢复运行产生的重复记录，只读取每个 `idx` 最后一次响应。
- 依次尝试从模型回答中提取：
  1. 直接 JSON 对象。
  2. 唯一 JSON 代码块。
  3. 文本中唯一、括号平衡的 JSON 对象。
- JSON 必须且只能包含：

```json
{
  "annotated_sentence": "..."
}
```

- 扫描 `<skill>` 和 `</skill>` 标签，拒绝：
  - 空标签
  - 嵌套标签
  - 重叠标签
  - 未闭合标签
  - 无对应开始标签的结束标签
- 删除标签后，原则上必须逐字符还原原始句子。
- 如果模型修改了标签外文本，但标签内的技能片段仍能唯一、顺序且不重叠地映射回原句，则执行 `exact_tag_payload_projection` 恢复。
- 若技能文本在原句中存在多个歧义位置，则解析失败。
- 为每个合法跨度生成：
  - `text`
  - `start`
  - `end`
  - `label_id=span:<idx>:<start>:<end>`

#### Output

逐采样解析结果：

```text
output/self_annotator/<run-id>/parsed/samples.jsonl
```

每条原始句子正常对应 5 条解析记录。

解析统计：

```text
output/self_annotator/<run-id>/parsed/summary.json
```

包括：

- `ok` 数量
- `recovered` 数量
- `failed` 数量
- 使用过的恢复方法
- 各类解析失败原因

### 1.1.5 consensus.py（存在性投票与跨度共识聚合）

#### Input

```text
output/self_annotator/<run-id>/parsed/samples.jsonl
```

聚合参数来自：

```text
config/self_annotator.json
```

#### Process

首先要求每条句子必须具有完整的 `sample_index=0..4` 结构，然后分两层聚合。

第一层：技能存在性投票——区分正负样本

- 有效解析少于 3 个：
  - 状态为 `abstained`
- 至少 4 个有效回答认为没有技能：
  - 状态为 `negative`
- 有技能票数少于 3，且不满足高置信负例：
  - 状态为 `unsolved`
- 有技能票数至少为 3：
  - 进入跨度聚合

五个回答全部有效时，对应关系为：

- 0～1 个有技能回答：`negative`
- 2 个有技能回答：`unsolved`
- 3～5 个有技能回答：进入正例跨度聚合

第二层：技能跨度聚合

- 使用''字符''起止位置区分标签；相同文本出现在不同位置时不会被合并。
- 同一精确跨度获得至少 3 票时直接接受，记为 `exact_vote`,与已接受跨度重叠的低票边界变体记为 `explained_variants`，不再单独处理。
- 其余跨度依据字符重叠关系组成跨度家族，家族支持度为该家族覆盖的不同采样编号数：
  - 支持度 1～2：作为噪声，不影响最终结果。
  - 支持度至少 3：调用 `hard_span_matcher.py`。
- 如果存在未解决跨度家族、最终跨度互相冲突，或正例没有稳定跨度，则整条句子标记为 `unsolved`。
- 只有所有跨度家族均解决且接受跨度之间不冲突时，状态才是 `accepted`。

同时计算不确定性指标：

- 回答之间的跨度集合 Jaccard。
- 平均 Jaccard。
- 标签熵。
- 各回答技能数量及其均值、最小值和最大值。

例如原句：

```text
Strong project planning skills.
```

5 次回答可能标成：

```text
① project planning
② project planning
③ planning
④ project planning skills
⑤ project planning
```

project planning​ 的字符边界获得了 3 票，因此直接接受，而planning和project planning skills会被记成explained_variants

而例如：

```text
样本0：project planning
样本1：project planning
样本2：planning
样本3：planning
样本4：无技能
```

票数分别是：

```text
project planning：2票
planning：2票
```

没有任何跨度达到精确接受所需的 3 票，但是两个跨度互相重叠，因此会被归入同一个跨度家族：

```text
跨度家族
├── project planning：样本0、样本1
└── planning：样本2、样本3
```

跨度家族本质上是一个重叠连通分量。只要跨度之间存在直接或间接的字符重叠，就可能进入同一家族。

随后计算家族支持度，其衡量的是家族覆盖的不同采样的数量。上面的例子的家族支持度就为：{0, 1, 2, 3}，即 4。

如果支持度大于3，则进入硬匹配阶段。

#### Output

最终共识：

```text
output/self_annotator/<run-id>/aggregated/consensus.jsonl
```

完整聚合审计：

```text
output/self_annotator/<run-id>/aggregated/aggregation_audit.jsonl
```

不确定性审计：

```text
output/self_annotator/<run-id>/aggregated/uncertainty_audit.jsonl
```

聚合汇总：

```text
output/self_annotator/<run-id>/aggregated/summary.json
```

### 1.1.6 hard_span_matcher.py（跨度家族硬匹配）

#### Input

- 一个由相互重叠的边界变体构成的跨度家族。
- 每个跨度的精确票数及来源采样编号。
- Hard match 配置。

#### Process

- 只处理支持度至少为 3、但没有任何单一跨度达到 3 票的家族。
- 按以下顺序选择锚点跨度：
  1. 精确票数更高。
  2. 对其他候选的最小覆盖率更高。
  3. 平均覆盖率更高。
  4. 技术词长度更短。
  5. 字符长度更短。
  6. 起止位置更靠前。
- 覆盖率计算：

```text
锚点覆盖率 = 锚点与另一跨度的重叠字符数 ÷ 锚点字符数
```

- 根据锚点长度动态提高阈值：

```text
threshold = min(0.85, 0.45 + 0.05 × (word_units - 1))
```

- 所有其他家族成员都达到覆盖阈值时，接受锚点跨度，标记为 `family_hard_match`。
- 任意成员未达到阈值时，家族保持未解决，原因记为 `family_anchor_overlap_failed`。

#### Output

该模块不单独写文件，其完整结果写入：

```text
aggregated/aggregation_audit.jsonl
```

包括：

- 锚点跨度
- 候选排序
- 动态阈值
- 两两覆盖率
- 是否通过 hard match
- 失败原因

### 1.1.7 selection.py（生成正式标注）

#### Input

```text
output/self_annotator/<run-id>/aggregated/consensus.jsonl
```

#### Process

- 将共识状态分为：
  - `accepted`
  - `negative`
  - `unsolved`
  - `abstained`
- 只有以下两类进入正式标注集：
  - `accepted`：`has_skill=1`，包含稳定技能跨度。
  - `negative`：`has_skill=0`，跨度列表为空。
- `unsolved` 和 `abstained` 不进入正式标注集，但保留在完整决策文件中。
- 为记录补充：
  - `schema_version`
  - `dataset_id`
  - `record_id`
  - `source_sha256`

#### Output

正式标注：

```text
output/self_annotator/<run-id>/selected/annotations.json
```

包含 `accepted` 和 `negative` 记录。

完整决策：

```text
output/self_annotator/<run-id>/selected/decisions.jsonl
```

包含所有四种状态，用于复核和下游筛选。

统计摘要：

```text
output/self_annotator/<run-id>/selected/summary.json
```

包括正式正例、正式负例、未解决记录、弃权记录和技能跨度数量。

### 1.1.8 validator.py（运行结果验证）

#### Input

- `config/self_annotator.json`
- `output/self_annotator/<run-id>/manifest.json`
- `selected/annotations.json`
- `selected/decisions.jsonl`
- `selected/summary.json`

#### Process

- 检查运行状态是否为：
  - `completed`
  - `completed_with_errors`
- 检查决策数量是否等于 Manifest 中锁定的输入数量。
- 检查正式标注数量是否与 Selection 汇总一致。
- 检查每条决策的：
  - `dataset_id`
  - `record_id`
  - `source_sha256`
- 计算最终决策文件 SHA256。

#### Output

验证结果，例如：

```json
{
  "run_id": "synthetic100-self-v1",
  "status": "valid",
  "records": 100,
  "formal": 96,
  "dataset_id": "...",
  "decisions_sha256": "..."
}
```

# 2 Offline Reaggregation——跳过模型调用，使用历史原始响应重新解析和聚合

## 2.1 run.ps1 -ReaggregateFrom

### 2.1.1 reaggregate.py

#### Input

一个历史自我标注运行目录，其中必须保留：

```text
<source-run>/
├── manifest.json
└── raw/
    └── responses.jsonl
```

#### Process

- 要求来源运行已经完成。
- 检查来源的标注协议与当前配置兼容。
- 检查每条句子仍然具有固定的 5 个采样。
- 记录来源 Manifest 和原始响应的 SHA256。
- 不调用任何网络接口。
- 重新执行：
  1. `parse_answers`
  2. `consensus`
  3. `selection`
- 处理结束后再次计算来源文件哈希，确保重聚合期间来源未变化。
- 新运行目录必须为空，禁止覆盖已有运行。

#### Output

```text
output/self_annotator/<new-run-id>/
├── raw/
│   └── source_reference.json
├── parsed/
├── aggregated/
├── selected/
└── manifest.json
```

Manifest 会记录：

- `run_kind=offline_reaggregation`
- `network_called=false`
- 来源运行 ID
- 来源文件路径及 SHA256
- 当前解析和聚合配置
